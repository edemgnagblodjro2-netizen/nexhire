import AlertBanner from '../components/AlertBanner';

export default function Communication() {
  return (
    <AlertBanner variant="b" icon="ti-message-circle">
      Module messagerie intégré — canaux par chantier, messages filtrés bureau/terrain, signalement incidents. Voir prototype dédié.
    </AlertBanner>
  );
}
